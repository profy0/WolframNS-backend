spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/WolframNS
    username: nikit
    password: 1112
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database: postgresql